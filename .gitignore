node_modules/
dist/
.DS_Store
Thumbs.db
*.log
.vite/
.claude/
backups/
.vercel

# Rebuilt from Neon before every build, but committed so a fresh
# clone builds without database access.
#src/generated/
.env*
!.env.example

# Switzer is pulled from Fontshare before dev/build (scripts/fetch-fonts.mjs).
# Its license forbids redistribution, and this repo is public — never commit it.
public/fonts/switzer/
